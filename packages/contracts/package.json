{
  "name": "@humanbase/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "On-chain Reclaim (zkTLS) delivery-proof verification for humanbase (BUSL-1.1)",
  "scripts": {
    "build": "forge build",
    "test": "forge test"
  }
}
